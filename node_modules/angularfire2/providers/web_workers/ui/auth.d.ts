import { ServiceMessageBrokerFactory } from 'angular2/platform/worker_render';
import { FirebaseSdkAuthBackend } from '../../firebase_sdk_auth_backend';
export declare class MessageBasedFirebaseAuth {
    private _sdkBackend;
    private _brokerFactory;
    constructor(_sdkBackend: FirebaseSdkAuthBackend, _brokerFactory: ServiceMessageBrokerFactory);
    start(): void;
    private _getAuth();
}
